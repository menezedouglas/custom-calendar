import { store as month } from '@/modules/month'
import { store as lang } from '@/modules/lang'
import { store as day } from '@/modules/day'
import { store as full } from '@/modules/full'

export default {
  month,
  lang,
  day,
  full
}
